type PageInfo {
    totalPageCount: Int!
    totalRowCount: Int!
    currentPageIndex: Int!
    currentRowCount: Int!
}

interface OffsetPagingData {
    pageInfo: PageInfo!
    data: [BaseModel!]!
}
