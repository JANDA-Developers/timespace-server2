type Item implements BaseModel {
    _id: ID!
    store: Store
    buyer: User
    product: Product
    code: String!
    name: String!
    memo: String
    dateTimeRange: DateTimeRange
    createdAt: DateTime!
    updatedAt: DateTime!
    expiresAt: DateTime
}
