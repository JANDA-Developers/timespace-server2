type Query {
    GetProductsByStoreCode(
        param: GetProductsByStoreCodeInput!
    ): GetProductsByStoreCodeResponse!
}

type GetProductsByStoreCodeResponse {
    ok: Boolean!
    error: Err
    data: [Product!]
}

input GetProductsByStoreCodeInput {
    storeCode: String!
}
