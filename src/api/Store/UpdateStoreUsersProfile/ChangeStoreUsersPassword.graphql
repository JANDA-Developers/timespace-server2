type Mutation {
    ChangePasswordStoreUser(
        oldPassword: String!
        newPassword: String!
    ): ChangePasswordStoreUserResponse!
}
type ChangePasswordStoreUserResponse {
    ok: Boolean!
    error: Err
}
