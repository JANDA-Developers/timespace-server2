type Mutation {
    ChangeStoreUsersPassword(
        oldPassword: String!
        newPassword: String!
    ): ChangeStoreUsersPasswordResponse!
}
type ChangeStoreUsersPasswordResponse {
    ok: Boolean!
    error: Err
}
