type Mutation {
    EmailSignIn(param: EmailSignInInput!): EmailSignInResponse!
}

type EmailSignInResponse {
    ok: Boolean!
    error: Err
    data: EmailSignInResult
}

input EmailSignInInput {
    email: EmailAddress!
    password: String!
}

type EmailSignInResult {
    token: String!
    expiresIn: Int!
}
