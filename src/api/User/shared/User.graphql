type User {
    # user.sub
    _id: ID!
    name: String!
    email: String!
    email_verified: Boolean!
    phone_number_verified: Boolean!
    phone_number: String!
    zoneinfo: String!
    tokenExpiry: Int!
}

type Query {
    users: [User!]
}
